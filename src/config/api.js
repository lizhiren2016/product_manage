
const loginApi = `/v1/login` // 登录
const registerApi = `/v1/register` // 注册
const usersApi = `/v1/users` // 用户
const productsApi = `/v1/products` // 产品
const countProductsApi = `/v1/products/count` // 按日期范围统计产品

export {
  countProductsApi,
  productsApi,
  usersApi,
  loginApi,
  registerApi
}
